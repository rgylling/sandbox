var assert = require('assert');
var compute = require('../js/app.js');

describe('--------', function() {
  // it('should check first question', function() {     assert.deepEqual(compute.getQuestion(0), 'first');   });
  it ('enter test condition here', function() {
          assert(true === true);
      }
  });
});   // describe 1
